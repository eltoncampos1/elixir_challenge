name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install Elixir
      uses: erlef/setup-elixir@v1
      with:
        elixir-version: '1.12'
        otp-version: '24.0'
    - name: Install Dependencies
      run: mix deps.get
    - name: Run Tests
      run: mix test